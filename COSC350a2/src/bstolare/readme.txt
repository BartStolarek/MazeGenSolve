//TODO:“readme” text file describing how to compile your program (python should be a single command)

//An optional third file that explains any command line arguments that we can use when running your program may be included


First requirement is to install the dependancies by opening the terminal, navigating to /bstolar directory, and run

'pip install -r requirements.txt'